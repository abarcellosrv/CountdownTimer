export default class Project {

	constructor(id, name, date, description,completed) {
		this.id = id;
		this.name = name;
		this.date = date;
		this.description = description;
		this.completed = completed;
	}
}